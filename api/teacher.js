const express = require('express');

const teacher = express.Router();


// teacher.get('/', (rep, res, next) => {
    
//     });
// });

// teacher.get('/classes', (rep, res, next) => {
     
//     });
// });

module.exports = teacher;